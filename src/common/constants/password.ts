/** Minimum accepted password length for registration, login payloads, and password changes. */
export const PASSWORD_MIN_LENGTH = 8

/** Maximum accepted password length to bound Argon2 hashing cost. */
export const PASSWORD_MAX_LENGTH = 128
